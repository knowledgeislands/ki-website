# Knowledge Islands repo config — read by the knowledgeislands-repo-config skill.
# One [table] per skill that needs per-repo options.
[knowledgeislands-repo-config]
visibility = "private"   # "public" | "private"
exceptions = []          # acknowledged check-ids, e.g. ["wiki"]
